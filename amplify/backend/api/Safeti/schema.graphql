type Dependents @model @auth(rules: [{allow: public}]) {
  id: ID!
  email: AWSEmail
  name: String
  location: String
}

type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  description: String
  isComplete: Boolean
}
 